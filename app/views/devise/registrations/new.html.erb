<% content_for :body_class, 'login-img-body' %>
<%= react_component "login", props = {
    user: current_user,
    loggedIn: user_signed_in?,
    url: registration_path(resource_name),
    resource: resource,
    name: resource_name,
    errors: devise_error_messages!,
    fieldType: "register"
} %>
