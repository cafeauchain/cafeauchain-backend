<% content_for :title, defined?(title) ? title : "This is the title" %>
<% props = local_assigns.except(:title, :nav, :scripts) %>
<%= react_component "main", props %>
<% if defined?(scripts) %>
    <% scripts.each do |s| %>
    <script src=<%= s %>></script>
    <% end %>
<% end %>
