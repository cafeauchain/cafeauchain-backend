const tableDefinition = {
    fields: [{ name: "date", label: "Date" }],
    props: {
        celled: true,
        striped: true,
        selectable: true,
        singleLine: true
    }
};

export default tableDefinition;
